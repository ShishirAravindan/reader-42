# Per-item runtime artifacts. The template ships with empty source/ output/
# but per-instance copies will fill these. They never round-trip back to the repo.
source/*
!source/.gitkeep
output/*
!output/.gitkeep
extracted/
cleaned/
manifest.json
tool_proposal.md

# CC session artifacts (created at runtime in each item's workspace)
.claude/projects/
